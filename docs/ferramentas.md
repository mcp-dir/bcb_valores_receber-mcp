# Ferramentas

Banco Central do Brasil (BCB): Valores a Receber expõe 1 ferramenta (todas somente leitura).

### 1. `bcb_valores_receber_consultar`
**Input**: `cpf` (opcional), `cnpj` (opcional), `data_nascimento` (opcional), `data_abertura_empresa` (opcional)

Banco Central do Brasil (BCB): Valores a Receber, consulta em fonte oficial.

## Prompts de exemplo

```
Consultar Banco Central do Brasil (BCB): Valores a Receber
```
